# AGENTS.md

小龙虾 U 盘版桌面客户端（Electron + React + TailwindCSS）。本文件是给在此仓库工作的 AI/协作者的约束清单，
先读完再动手；与既有代码风格冲突时以本文件为准。

## 一、代码规则（硬性）

1. **不写墓碑注释**：不写"这里原来是…""已改为…""第 N 版删除…"这类历史痕迹，注释只描述当前意图与约束。
2. **头部注释**：每个文件、每个函数都要有一行说明用途的头部注释；关键变量（状态、常量、缓存）也要有注释说明它代表什么。
3. **无冗余**：没用的变量、函数、文件、依赖当场删除，不留死代码、不留"以防万一"的分支；重构过程中产生的重复实现（如两份相同的工具函数）要合并成一份。
4. **原地重构**：直接改原文件原函数，不新增 `V2`/`new`/`old` 式命名，不保留两套实现。
5. **命名要准确**：标识符要表达业务含义（用 `openclawConfig`、`subscriptionProvider` 这类），不要 `data1`、`api2`、`window.xlxApi` 这类糊弄式命名；资源文件用英文名。
6. **注释与交流用中文**：注释、提交信息、给用户的回复都用中文。

## 二、架构约束

- **纯 IPC**：渲染进程只通过 `window.zgy`（preload 的 contextBridge）与主进程通信；除 OAuth 登录期间临时监听 `127.0.0.1` 回调端口外，不起任何本地 HTTP 服务。
- **无内置 Node**：子进程统一用 Electron 自带 Node（`ELECTRON_RUN_AS_NODE=1`），不要引入额外运行时。
- **运行期文件统一放 `data/`**：授权文件、配置、日志、凭证、按需下载的插件包、更新包都在 exe 同级的 `data/` 下，随 U 盘插拔；不要在别处新建持久化目录。
- **时间与超时统一在 `src/shared/timing.json`**：所有轮询间隔、等待上限、冷却时间都从那里取值，主进程用 `require`、渲染层直接 `import` JSON，禁止在业务文件里写死数字（看护进程通过环境变量下发）。
- **`writeConfig` 是合并语义**：`deepMerge(磁盘, 输入)`，输入里缺省的键会保留磁盘旧值——要清掉一个键或数组元素必须显式写回目标值（如空数组），对输入 `delete` 删不掉磁盘值；只有出厂重置用 `{ replace: true }` 整体替换。类似"受限模式下不得出现的哨兵值"（如飞书 allowFrom 的 `"*"`）要在 `normalizePortableFields` 里加守卫兜底。
- **面板异步按钮统一用 `AsyncButton`**：保存/刷新/批准/扫码这类会等待的操作，点击后按钮自动置灰转圈并换成"xx中..."文案，完成后恢复——不让用户对耗时操作（如冷启动等二维码、网关重启）毫无感知，也防重复提交。
- **读写插件落盘文件前先确认其实际路径**：openclaw（pairing-store 等）把凭据类文件放在 **state 目录**（`data/.openclaw/credentials/`，如 `feishu-pairing.json`、`feishu-default-allowFrom.json`），不是 `data/credentials/`；拿不准就先看插件源码或实际落盘位置，别按目录名想当然。
- **颜色统一走设计令牌**：色值只在 `src/renderer/src/styles/global.css` 的 `:root` 变量里出现，`tailwind.config.js` 映射为语义类；页面里不要写死颜色。
- **前端对齐 2.0 原型**：只参考原型的布局与逻辑，不复用旧版实现；样式优先用 Tailwind 语义类，不用行内样式兜底。
- **长耗时操作盖加载页**：会等十几秒以上的操作（首次冷启动、插件解压等）统一用 `LoadingScreen` 整窗遮罩并写明原因，不要只留一句"生成中"让人以为卡死；"是不是首次"用主进程给的事实标记（`.zgy-warm`）判断，不要凭等了几秒去猜。
- **扫码通道的换码策略统一走 `services/qr-session.js`**：状态、二维码缓存、过期自动重建（冷却与连续次数上限取 `timing.qrSession`）、等待与变更通知都在那里；各通道只提供"怎么发起一次尝试、怎么结束当前尝试"（微信拉子进程、QQ 调官方连接器），不要再各写一份计数与冷却。面板在自动换码期间保持轮询并重绘新码。
- **通道面板工作区只有一份**：图标栏 + 面板区抽成 `ChannelWorkbench`，向导 BOT 页与运行页"通道设置"共用；通道面板必须能在网关运行中操作（批准配对、切策略、改凭据、重绑）。**保存/扫码绑定只写配置并 `markConfigPendingRestart` 登记，不逐次重启网关**——界面（工作区底部与运行页）按 `gateway:status` 的 `pendingRestart` 显示「重启生效」按钮，用户改完多个平台后点一次统一应用；标记在网关真正启动时清空。
- **"聊天工具"清单按已配置取，不按启用位/选中项拼**：确认页与运行页的通道列表一律来自 `channelSummary()`（扫码绑上或凭据填全才算），向导里仅选中没配置的不展示；飞书按所选域名显示"飞书"或"Lark"。向导保存前必须重新 `config:load` 取最新值——主进程随时在落盘，回写进向导时的旧快照会清掉 BOT 页里刚写入的凭据；通道启用位由各通道在扫码/保存成功时自行写入，不在保存流程里按"当前选中"启用。
- **通道插件只登记一条加载路径**：优先 `resources/plugins/<插件名>`，没有再回退到模块缓存里的同名包；模块包里没有的插件走 `resources/payload/` 的按需包（登记表 `PAYLOADS` + `ensurePayload()`）。同一插件登记两条路径会触发 openclaw 的 `duplicate plugin id` 告警。**企业微信是例外**：走 openclaw 官方安装布局——payload 解压到 `data/.openclaw/extensions/` 由 openclaw 自动发现（不登记加载路径），`node_modules/openclaw` 依赖链接指向本机模块缓存、换机器激活时由 `installWecomPlugin()` 重建，因此 payload 包里不能带这个链接。
- **打包边界**：`src/main` 会被 bytenode 编译成字节码，`preload` 必须保持明文源码（渲染进程在启用 asar 完整性校验的包内加载字节码会崩溃）；`src/shared` 等被主进程 `require` 的额外目录要记得加进 `package.json` 的 `build.files`。

## 三、产品约束（不要擅自扩大）

- **微信通道保持单账号**：一个 U 盘同时只服务一个微信号。"重新绑定"是**替换**语义 ——
  新号绑成功后由客户端清理旧号数据（索引、凭据、同步缓存、上下文令牌、授权名单），
  并且只在绑定成功后清理，点击时不清理（用户可能点了不扫）。
  不做多账号列表/切换/并存，不要为其加 UI 或配置。
- **通道面板的状态要能跨页签、跨重启保留**：已绑定状态从落盘数据推导，不依赖内存态。

## 四、工作方式

- **不自动提交 git**：改完让用户审阅，用户明确说"提交"再提交；提交时只包含用户指定的范围（例如只提插件目录），其余改动留在工作区。
- **不要留在仓库里的临时产物**：截图脚本、临时探针、备份文件、日志写到仓库外的临时目录（`/tmp` 等），不要在仓库根留下 `.cap.ps1`、`tests/` 这类自造文件。
- **验证要真跑**：能跑就跑（构建、启动应用、调接口、看日志、截图），不要靠"看代码应该没问题"下结论；改动涉及界面时给截图证据，涉及接口时给实际响应。做不到的部分（例如需要用户手机扫码、需要外部平台在线）要如实说明。
- **单进程独占某窗口时别抢焦点**：验证界面用截图/无障碍树读取，不要为了点击而把窗口反复抢到前台。

## 五、交付与文档

- `README.md`：客户端本身的开发、打包、授权、架构。
- `docs/运行部署手册.md`：启动前后端、授权配置、打包拷盘、更新、心跳与令牌保活、轮询与超时总表、常见问题。改动涉及这些内容时同步更新。
- 出问题先看日志：`data/.openclaw/logs/`（网关、通道、后台联检、微信登录里还有 `qr ready in Xms` 这类耗时记录）；
  怀疑配置被改坏时看 `config-writes.log`（每次写配置一行：调用点 + provider/Key 概览 + 是否触发防丢护栏）。

## 六、常用命令

```bash
npm run dev          # 开发：Vite(5183) + Electron，渲染层热更新
npm run build:renderer && npx electron .   # 生产模式预览
npm run dist:win     # 打包 Windows 单文件 portable → release/小龙虾U盘版.exe
npm run dist:mac     # 打包 macOS dmg（只能在 macOS 上跑）
```

打包流水线会临时把 `src/main` 编译成字节码再恢复，工作区始终应是明文源码；若发现源码被替换成存根，
执行 `git checkout -- src/` 还原。
